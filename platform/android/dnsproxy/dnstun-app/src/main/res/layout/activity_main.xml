<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            xmlns:compat="http://schemas.android.com/apk/res-auto"
            android:id="@+id/activity_main"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:scrollbars="vertical"
            tools:context="com.adguard.testapp.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >
        <Space
            android:layout_width="match_parent"
            android:layout_height="48sp"
            />
        <ImageButton
            android:id="@+id/startstop_button"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_gravity="center"
            android:src="@drawable/power_md"
            android:contentDescription="@string/app_name"
            android:scaleType="centerInside"
            android:background="@drawable/button_background_gray"
            android:elevation="4dp"
            />

        <Space
            android:layout_width="match_parent"
            android:layout_height="24sp"
            />

        <CheckBox
            android:id="@+id/pcap_button"
            android:layout_width="wrap_content"
            android:layout_height="20dp"
            android:text="Save Pcap file (check before start)"
            android:layout_gravity="center"
            android:layout_marginLeft="0sp"
            android:layout_marginRight="16sp"
            />

        <Space
            android:layout_width="match_parent"
            android:layout_height="16sp"
            />
        <TextView
            android:id="@+id/interface_info_textview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16sp"
            android:textSize="16sp"
            android:text="@string/app_name"
            android:textColor="#111111"
            />
    </LinearLayout>
</ScrollView>
