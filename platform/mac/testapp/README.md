# Old macOS/iOS sample app (deprecated)

This is the old macOS/iOS sample app. It is **deprecated** and kept for reference only: it is not developed anymore.

Use [`../DnsLibsTestApp`](../DnsLibsTestApp) instead — it is the current sample app and covers the same providers
(`NEPacketTunnelProvider` and `NEDNSProxyProvider`, the latter as a macOS system extension).

See [docs/macos-testapp.md](../../../docs/macos-testapp.md) for how to build and run this app and for the current one.
