diff --git a/lib/multi.c b/lib/multi.c
index 6dfe8842e..9c53a857b 100755
--- a/lib/multi.c
+++ b/lib/multi.c
@@ -573,6 +573,12 @@ static CURLcode multi_done(struct Curl_easy *data,
     DEBUGF(infof(data, "Connection still in use %zu, "
                  "no more multi_done now!\n",
                  conn->easyq.size));
+
+    // If current Curl_easy is owner, deown.
+    if (conn->data == data) {
+      conn->data = NULL;
+    }
+
     return CURLE_OK;
   }
 
